# Outstanding Changes Audit

## Result

- `main` is synchronized with `origin/main` (`+0/-0`); there are no unpushed commits.
- One file is staged: `modules/features/system-storage.nix`.
- Ten tracked files have unstaged modifications: `flake.lock`, three host configurations, and six generated `graphify-out/` files.
- Pre-existing untracked content consists of `.atl/` (two generated skill-registry files), `.opencode/plans/1787228382479-lucky-river.md`, and three `graphify-out/cache/ast/` files.

## Change Groups

1. Root-filesystem protection:
   - Add the shared `systemStorage` module with weekly Nix GC and optimisation, free-space thresholds, and bootloader generation limits.
   - Import it from `azula`, `toph`, and `my-machine`.
   - Give `azula` larger 10/20 GiB free-space thresholds than the shared 5/10 GiB defaults.
2. Dependency refresh:
   - `flake.lock` updates seven inputs, independently of the storage change.
3. Generated/support artifacts:
   - The untracked storage implementation plan documents diagnosis, implementation, cleanup, and verification.
   - `.atl/` contains an auto-generated skill registry and fingerprint.
   - `graphify-out/` was regenerated to index those files and the storage plan.

## Important State

The new module is staged, but all three host imports are unstaged. A commit of the current index alone would therefore add an unused module without wiring it into any host.

## Verification

- Inspected `git status --porcelain=v2 --branch`.
- Inspected staged and unstaged diffs separately.
- Confirmed `git log @{upstream}..HEAD` is empty.
- No builds, evaluations, staging changes, or configuration edits were performed.
